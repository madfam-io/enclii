apiVersion: v1
kind: Service
metadata:
  name: claude-ui
  namespace: madfam-automation
  labels:
    app: claude-ui
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3001
      protocol: TCP
      name: http
  selector:
    app: claude-ui
