apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: madfam-automation

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - hpa.yaml
  - pdb.yaml
  - networkpolicy.yaml

# ArgoCD Image Updater manages this via git write-back
images:
  - name: ghcr.io/madfam-org/claudecodeui
    newTag: latest
